services:
  - type: web
    name: aura-backend
    env: node
    plan: free
    region: oregon
    buildCommand: npm install
    startCommand: npm start
    rootDir: server
    autoDeploy: true
    envVars:
      - key: PORT
        value: 10000
